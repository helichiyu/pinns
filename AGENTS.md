# AGENTS.md

减少常见 LLM 编码错误的行为准则。

**原则：** 宁可谨慎，不要冒进。简单任务可灵活处理。

## 1. 先想清楚再写代码

**不要假设。不要掩盖困惑。主动暴露权衡。**

动手之前：

- 明确说出你的假设。不确定就问。
- 如果有多种理解，全部列出来，不要自己悄悄选一个。
- 如果有更简单的方案，说出来。该反驳时就反驳。
- 如果有什么不清楚的，停下来。说清楚哪里不懂，然后问。

## 2. 简单优先

**用最少的代码解决问题。不做多余的。**

- 不做超出需求的功能。
- 不为只用一次的代码搞抽象。
- 不做没被要求的“灵活性”或“可配置性”。
- 不为不可能发生的情况写错误处理。
- 如果你写了 200 行，而 50 行就能搞定，重写。

问自己：“一个资深工程师会觉得这太复杂了吗？”如果是，就简化。

## 3. 精准修改

**只改必须改的。只清理自己制造的混乱。**

修改现有代码时：

- 不要“顺手改进”相邻的代码、注释或格式。
- 不要重构没坏的东西。
- 匹配现有风格，即使你自己的偏好不同。
- 如果发现无关的死代码，提一句，不要删。

当你的修改产生了废弃代码：

- 删除因你的修改而变得无用的导入、变量和函数。
- 不要删除之前就存在的死代码，除非用户要求。

检验标准：每一行改动都应该能追溯到用户的请求。

## 4. 目标驱动执行

**定义成功标准。循环直到验证通过。**

把任务转化为可验证的目标：

- “添加验证” → “为无效输入写测试，然后让测试通过”
- “修 Bug” → “写一个能复现的测试，然后让测试通过”
- “重构 X” → “确保重构前后测试都通过”

多步骤任务，先列出简要计划：

```text
1. [步骤] → 验证：[检查方式]
2. [步骤] → 验证：[检查方式]
3. [步骤] → 验证：[检查方式]
```

清晰的成功标准可以让你独立循环推进。模糊的标准（“让它能用”）则需要反复确认。

## 5. 代码质量与优雅

**代码要写得优雅，但优雅不等于复杂。**

- 优先写清晰、直接、可维护的代码。
- 命名要准确表达意图，避免含糊缩写和无意义命名。
- 函数职责要单一，控制流要容易读懂。
- 消除真实重复，但不要为了消除表面重复制造复杂抽象。
- 优先让核心逻辑自然表达业务含义，而不是堆叠技巧。
- 错误处理要服务于真实风险，不要用宽泛异常掩盖问题。
- 代码改完后要自查可读性：半年后接手的人是否能快速理解。

### 科研代码原则

- **功能实现优先**：先保证功能正确，再考虑代码优化。
- **保持简单直接**：避免过度抽象和设计模式，可读性第一。
- **配置集中管理**：所有配置参数统一放在 `config.py`，便于调试。

### 代码书写规范

1. **代码注释均使用英文**：所有函数注释、行内注释、文档字符串都必须使用英文。
2. **UI 界面内容必须使用中文**：前端 UI 中的所有用户可见文本、标签、按钮、图例和提示都必须使用中文，不允许使用 emoji 表情符号。
3. **变量命名使用英文**：函数名、变量名、类名等标识符使用英文，遵循 Python 命名约定。

## 6. 中文与编码

- 所有问题都用中文回答。
- 在 Windows PowerShell 5.1 中读取中文或 UTF-8 文本文件时，必须显式指定编码，例如：
  - `Get-Content -Path <file> -Encoding UTF8`
  - `Set-Content -Path <file> -Encoding UTF8`
- 不要用裸 `Get-Content <file>` 读取 `todos/*.md`、中文 Markdown、中文 JSON 或其它 UTF-8 文本；PowerShell 5.1 会把无 BOM UTF-8 当作系统 ANSI 解码，导致中文乱码。
- 如果需要在 PowerShell 中运行 Python/pytest 并输出中文，优先设置：
  - `$env:PYTHONIOENCODING='utf-8'`

## 7. TODO 管理流程规范

### 需求收集阶段

当用户提出修改要求时，遵循以下流程：

1. **详细追问**：对用户的需求进行不断追问，直到以下方面都明确：
   - 具体功能要求和预期效果
   - UI/UX 设计要求和交互逻辑
   - 数据结构和存储要求
   - 性能和兼容性要求
   - 验收标准和测试方法
2. **方案制定**：基于明确的需求，制定详细的实施方案。
3. **TODO 文件生成**：将方案保存为结构化的 TODO 文件。

### TODO 文件命名规范

- 文件格式：`todos/todo_MMDD_N.md`
- `MMDD`：当天日期，如 `0905` 表示 9 月 5 日。
- `N`：当天第几次需求，从 1 开始递增。
- 示例：`todos/todo_0905_1.md`、`todos/todo_0905_2.md`

### TODO 文件结构

```markdown
# TODO: [需求标题] - [日期]

## 需求描述

[详细需求描述]

## 实施方案

[整体技术方案]

## 任务清单

### 阶段1: [阶段名称]

- [ ] 任务1: [具体任务描述]
- [ ] 任务2: [具体任务描述]

### 阶段2: [阶段名称]

- [ ] 任务3: [具体任务描述]
- [ ] 任务4: [具体任务描述]

## 验收标准

[明确的验收标准]

## 执行记录

[执行过程中的记录和备注]
```

### 执行阶段流程

当用户要求执行项目时：

1. **按序执行**：严格按照 TODO 文件中的方案流程顺序执行。
2. **实时更新**：每完成一项任务，立即更新 TODO 文件：
   - 将完成的任务标记为 `- [x]`
   - 在执行记录中添加完成时间和备注
   - 记录遇到的问题和解决方案
3. **支持中断**：支持用户随时中断修改计划。
4. **断点续传**：支持在之前的记录基础上继续完成后续工作。

### 状态管理

- `[ ]`：未开始
- `[进行中]`：正在执行
- `[x]`：已完成
- `[跳过]`：跳过执行
- `[阻塞]`：遇到阻塞问题

### 执行记录格式

```markdown
## 执行记录

### 2024-09-05 14:30

- 完成任务1: 创建用户界面组件
- 备注: 使用 Material Design 图标，符合 UI 设计规范

### 2024-09-05 15:45

- 开始任务2: 实现数据存储逻辑
- 问题: 需要确认数据结构格式
```
