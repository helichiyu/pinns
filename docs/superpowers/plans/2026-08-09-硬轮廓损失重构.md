# 硬轮廓损失重构 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把四项损失改成三项——轮廓从"逐轮可导的软包络"换成"占比锁死的硬 mask"，直方图从整图收窄到轮廓内，轮廓占比损失删除。

**Architecture:** 轮廓拆成两个函数：源图侧一次性硬阈值二值化算出整数 `k`，输出侧每轮模糊后取前 `k` 个最亮像素散射成 mask。`k` 而非占比 ρ 锁死两者，使两个 mask 的像素数严格相等，轮廓内直方图无需长度补偿。mask 是 `requires_grad=False` 的纯常量，只当选择器用，梯度经 `prediction` 回流。

**Tech Stack:** Python 3.12 / PyTorch 2.9 (CUDA) / tornado / matplotlib 3.10 / pytest 8.3.4 / 原生 JS

## Global Constraints

- Python 解释器一律 `D:/anaconda3/envs/use/python.exe`；pytest 用 `python -m pytest` 调用（Scripts 目录不在 PATH）。
- 所有命令的工作目录都是项目根 `e:/BM/迭代模型/损失`。
- 代码注释、文档、提交说明一律中文。
- SHARE 默认值：直方图 `0.5`、背景 `0.10`。振幅权重恒为 1。
- 标定公式不变：`β_i = SHARE_i × L_amp,0 / L_i,0`。
- **不做旧结果兼容**。`history.csv` 少一列、`convergence.png` 从 11 面板变 10 面板，旧 run 单独看即可。
- 因本次改动而失效的代码一律删除，不留死代码。
- 本次不动：`backend/model.py` 全文、迭代式输入（上轮输出 detach 后作为下轮输入）、`register_to_source` 的平移镜像消歧、`ITERATIONS` / `LEARNING_RATE` / `BASE_CHANNELS` / `LOG_EVERY` / `AMPLITUDE_FLOOR` / `HISTOGRAM_BINS` / `HISTOGRAM_SOFTNESS` / `CONTOUR_SIGMA` / `CONTOUR_THRESHOLD` 的取值。
- `adaptive_weighting_research.md` 是历史实测记录，不是当前行为描述，**不改**。

## 文件结构

| 文件 | 职责 | 本次 |
|---|---|---|
| `backend/losses.py` | 全部损失与轮廓函数 | 改：新增两个轮廓函数、CDF 拆出复用、直方图收 mask、权重降为两项；删三个失效函数 |
| `backend/train.py` | 训练主循环、CLI、离线指标 | 改：接线新损失、k=0 守卫、CLI 新增两个 SHARE、`HISTORY_KEYS` 去掉 `area_ratio` |
| `backend/visualization.py` | matplotlib 出图与 CSV | 改：收敛面板 11→10、支撑图标题、新增预览出图函数、后端渲染改 Agg |
| `backend/server.py` | tornado 服务、实验队列 | 改：图片目录扫 `images/`、预览接口改拼图返回 ratio、透传两个 SHARE |
| `backend/config.py` | 集中式配置（已 gitignore） | 改：SHARE 常量降为两个、`IMAGE_PATH` 加 `images/` 前缀 |
| `frontend/app.js` | 界面构建与 WebSocket | 改：参数区四行、宽度交给布局、按钮统一名、getConfig 新增字段 |
| `frontend/style.css` | 样式 | 改：三条规则让输入框均分行宽 |
| `tests/conftest.py` | 让测试能 import backend 模块 | **新建** |
| `tests/test_losses.py` | 轮廓与损失的不变式断言 | **新建** |
| `images/` | 存放数据图片 | **新建**，移入 `123.png` / `567.png` |

## 任务依赖

Task 1 → Task 2 → Task 3 → Task 4 → Task 5 → Task 6，严格顺序。

**Task 1 结束时 `train.py` 与 `server.py` 处于无法运行状态**（引用了已删除的函数），这是预期的：Task 2 修好训练脚本，Task 3 修好服务端。中途不要试图端到端跑。

---

### Task 1: 轮廓函数、轮廓内直方图与两项权重

**Files:**
- Modify: `backend/losses.py`（全文重写）
- Create: `tests/conftest.py`
- Create: `tests/test_losses.py`
- Modify: `.gitignore:1`（追加 `.pytest_cache/`）

**Interfaces:**
- Consumes: 无（本任务是起点）
- Produces:
  - `source_contour(source, sigma, threshold) -> Tensor`：形状同 `source`，取值 `{0.0, 1.0}`，dtype 同 `source`
  - `topk_contour(prediction, sigma, k) -> Tensor`：形状同 `prediction`，恰好 `k` 个 1，`requires_grad is False`
  - `soft_cdf(values, bins, softness) -> Tensor`：形状 `(bins,)`
  - `masked_histogram_cdf_loss(prediction, prediction_mask, target_cdf, bins, softness) -> Tensor`：标量
  - `background_loss(prediction, contour) -> Tensor`：标量
  - `CalibratedWeights(share_histogram, share_background)`，方法 `total(amplitude, histogram, background)` 与 `calibrate(amplitude, histogram, background)`
  - 保持原样：`gaussian_blur`、`amplitude_mask`、`normalized_log_amplitude_loss`
  - **已删除**：`dynamic_contour`、`soft_histogram_cdf_loss`、`contour_area_ratio_loss`

- [ ] **Step 1: 建 tests/conftest.py，让测试能 import backend 模块**

`backend/` 不是 package，测试需要把它加进 `sys.path`。

新建 `tests/conftest.py`：

```python
"""让 tests/ 下的测试能直接 import backend 里的模块。"""

import os
import sys

sys.path.insert(0, os.path.join(os.path.dirname(os.path.dirname(os.path.abspath(__file__))), "backend"))
```

- [ ] **Step 2: 写失败的测试**

新建 `tests/test_losses.py`：

```python
import torch

from losses import (CalibratedWeights, background_loss,
                    masked_histogram_cdf_loss, soft_cdf, source_contour,
                    topk_contour)

SIGMA = 4.0
THRESHOLD = 0.20
BINS = 64
SOFTNESS = 0.02


def make_disk(size=64, radius=14):
    """造一张合成图：暗背景上一个亮圆盘，亮度从中心向外递减。"""
    axis = torch.arange(size, dtype=torch.float32) - (size - 1) / 2
    distance = (axis[:, None].square() + axis[None, :].square()).sqrt()
    disk = (1.0 - distance / radius).clamp_min(0.0)
    return disk[None, None]


def test_source_contour_is_binary():
    """源图轮廓只能取 0 或 1。"""
    contour = source_contour(make_disk(), SIGMA, THRESHOLD)
    assert torch.equal(contour, contour.round())
    assert contour.amin() == 0.0
    assert contour.amax() == 1.0


def test_topk_contour_has_exactly_k_pixels():
    """输出轮廓的 1 像素数恰好等于 k。"""
    prediction = torch.rand(1, 1, 64, 64)
    for k in (1, 37, 500):
        contour = topk_contour(prediction, SIGMA, k)
        assert int(contour.sum().item()) == k


def test_topk_contour_selects_brightest():
    """选中的必须是模糊图里最亮的那批像素：mask 内的最小值不低于 mask 外的最大值。"""
    from losses import gaussian_blur
    prediction = torch.rand(1, 1, 64, 64)
    blurred = gaussian_blur(prediction, SIGMA)
    contour = topk_contour(prediction, SIGMA, 300)
    inside = blurred[contour > 0.5]
    outside = blurred[contour < 0.5]
    assert inside.amin() >= outside.amax()


def test_masks_have_equal_pixel_count():
    """源图 mask 与输出 mask 的像素数严格相等——这是轮廓内直方图不需要长度补偿的前提。"""
    source = make_disk()
    source_mask = source_contour(source, SIGMA, THRESHOLD)
    k = int(source_mask.sum().item())
    prediction_mask = topk_contour(torch.rand_like(source), SIGMA, k)
    assert int(source_mask.sum().item()) == int(prediction_mask.sum().item())


def test_topk_contour_is_constant():
    """mask 是纯常量，不参与求导。"""
    prediction = torch.rand(1, 1, 64, 64, requires_grad=True)
    contour = topk_contour(prediction, SIGMA, 200)
    assert contour.requires_grad is False


def test_gradient_flows_to_prediction():
    """mask 不可导，但梯度必须经 prediction 回流到网络。"""
    source = make_disk()
    source_mask = source_contour(source, SIGMA, THRESHOLD)
    k = int(source_mask.sum().item())
    target_cdf = soft_cdf(source[source_mask > 0.5], BINS, SOFTNESS)

    prediction = torch.rand_like(source).requires_grad_(True)
    contour = topk_contour(prediction, SIGMA, k)
    loss = (background_loss(prediction, contour)
            + masked_histogram_cdf_loss(prediction, contour, target_cdf, BINS, SOFTNESS))
    loss.backward()
    assert prediction.grad is not None
    assert prediction.grad.norm().item() > 0.0


def test_masked_histogram_zero_for_identical_distribution():
    """同一张图与自己比，轮廓内分布完全一致，损失应约为 0。"""
    source = make_disk()
    mask = source_contour(source, SIGMA, THRESHOLD)
    target_cdf = soft_cdf(source[mask > 0.5], BINS, SOFTNESS)
    loss = masked_histogram_cdf_loss(source, mask, target_cdf, BINS, SOFTNESS)
    assert loss.item() < 1e-12


def test_background_loss_zero_outside_contour():
    """轮廓外全为 0 时背景损失为 0；轮廓外有亮度时严格为正。"""
    contour = torch.zeros(1, 1, 8, 8)
    contour[..., 2:6, 2:6] = 1.0
    clean = contour.clone()
    assert background_loss(clean, contour).item() == 0.0
    dirty = clean.clone()
    dirty[..., 0, 0] = 0.5
    assert background_loss(dirty, contour).item() > 0.0


def test_calibrate_makes_contributions_match_shares():
    """标定后，两项的加权贡献应恰好等于 SHARE × 振幅损失。"""
    weights = CalibratedWeights(0.5, 0.10)
    amplitude = torch.tensor(2e-4)
    histogram = torch.tensor(3e-2)
    background = torch.tensor(7e-3)
    weights.calibrate(amplitude, histogram, background)
    contributions = weights.betas * torch.stack([histogram, background])
    assert torch.allclose(contributions, amplitude * torch.tensor([0.5, 0.10]), rtol=1e-5)
    total = weights.total(amplitude, histogram, background)
    assert torch.allclose(total, amplitude * 1.60, rtol=1e-5)
```

- [ ] **Step 3: 运行测试确认失败**

```bash
cd "e:/BM/迭代模型/损失" && D:/anaconda3/envs/use/python.exe -m pytest tests/test_losses.py -q
```

Expected: 收集阶段就报 `ImportError: cannot import name 'source_contour' from 'losses'`（新函数还不存在）。

- [ ] **Step 4: 重写 backend/losses.py**

全文替换为：

```python
import math

import torch
import torch.nn as nn
import torch.nn.functional as F


def gaussian_blur(image, sigma):
    radius = int(math.ceil(3 * sigma))
    axis = torch.arange(-radius, radius + 1, device=image.device, dtype=image.dtype)
    kernel = torch.exp(-(axis[:, None].square() + axis[None, :].square()) / (2 * sigma**2))
    kernel = (kernel / kernel.sum()).unsqueeze(0).unsqueeze(0)
    return F.conv2d(image, kernel, padding=radius)


def amplitude_mask(target_amplitude, relative_floor):
    mask = target_amplitude >= target_amplitude.amax() * relative_floor
    mask = mask.clone()
    mask[..., 0, 0] = False
    return mask


def normalized_log_amplitude_loss(prediction, target_amplitude, valid_mask):
    prediction_amplitude = torch.abs(torch.fft.fft2(prediction))
    scale = target_amplitude.amax().detach().clamp_min(1e-8)
    prediction_log = torch.log1p(prediction_amplitude / scale)
    target_log = torch.log1p(target_amplitude / scale)
    return F.mse_loss(prediction_log[valid_mask], target_log[valid_mask])


def source_contour(source, sigma, threshold):
    """源图硬轮廓，整个训练只算一次。

    模糊 → 减去自身最低基线 → 峰值归一化 → 硬阈值。减基线是为了避免整体亮度
    把轮廓撑到全图；阈值是相对峰值的比例，所以不随图片整体亮度漂移。
    """
    blurred = gaussian_blur(source, sigma)
    contrast = blurred - blurred.amin()
    return (contrast / contrast.amax().clamp_min(1e-6) > threshold).to(source.dtype)


def topk_contour(prediction, sigma, k):
    """输出侧硬轮廓：模糊后取前 k 个最亮像素置 1。

    像素数恒为 k，与源图轮廓严格相等，所以轮廓内直方图两侧长度天然一致。
    用 topk 的下标散射而不是阈值比较，是因为并列值在阈值下会多圈几个像素。

    mask 的 requires_grad 为 False，是纯常量，只当选择器用——它本身不可导也不
    需要可导，梯度经 prediction 回流到网络。
    """
    blurred = gaussian_blur(prediction, sigma).flatten()
    mask = torch.zeros_like(blurred)
    mask[torch.topk(blurred, k).indices] = 1.0
    return mask.view_as(prediction)


def soft_cdf(values, bins, softness):
    """一组像素值的可微软 CDF，长度为 bins。"""
    centers = torch.linspace(0, 1, bins, device=values.device, dtype=values.dtype)
    return torch.sigmoid((centers[:, None] - values[None, :]) / softness).mean(dim=1)


def masked_histogram_cdf_loss(prediction, prediction_mask, target_cdf, bins, softness):
    """只在轮廓内比较分布。

    预测分布取自输出自己的 mask，而不是源图 mask——后者等于把源图轮廓的位置
    当成监督。target_cdf 由调用方用源图 mask 预先算好，每轮复用。
    """
    values = prediction[prediction_mask > 0.5]
    return F.mse_loss(soft_cdf(values, bins, softness), target_cdf)


def background_loss(prediction, contour):
    """轮廓外的输出被约束为 0。contour 已是常量，无需再 detach。"""
    return torch.mean(((1.0 - contour) * prediction).square())


class CalibratedWeights(nn.Module):
    """固定权重的损失组合，权重由首步前向自动标定。

        L = L_amp + β_hist·L_hist + β_bg·L_bg

    振幅是唯一的硬数据，权重恒为 1。另两项是静态罚项，β 在第一次前向定一次，
    使各项的初始加权贡献等于 SHARE_i × L_amp,0。SHARE_i 按各约束的可信度排定
    （振幅 > 轮廓内直方图 > 背景），是全局常数而非每图超参：换图片时各 L_i,0
    都变、β 跟着变，但贡献占比不变。

    没有任何一项带自适应拉格朗日乘子。早先给占比项配对偶变量做梯度上升，收敛
    末期 θ 与 λ 互相追逐形成极限环，损失上下振荡；静态罚项没有这个反馈回路。
    """

    def __init__(self, share_histogram, share_background):
        super().__init__()
        self.shares = (share_histogram, share_background)
        self.register_buffer("betas", torch.zeros(2))   # 顺序：直方图、背景

    def total(self, amplitude, histogram, background):
        weighted = self.betas * torch.stack([histogram, background])
        return amplitude + weighted.sum()

    @torch.no_grad()
    def calibrate(self, amplitude, histogram, background):
        """首步标定：β_i = SHARE_i × L_amp,0 / L_i,0。"""
        anchor = amplitude.detach().clamp_min(1e-12)
        initial = torch.stack([loss.detach() for loss in (histogram, background)])
        shares = torch.tensor(self.shares, device=initial.device, dtype=initial.dtype)
        self.betas.copy_(shares * anchor / initial.clamp_min(1e-12))
```

- [ ] **Step 5: 运行测试确认全绿**

```bash
cd "e:/BM/迭代模型/损失" && D:/anaconda3/envs/use/python.exe -m pytest tests/test_losses.py -v
```

Expected: 9 passed。

若 `test_masked_histogram_zero_for_identical_distribution` 失败且值极小（如 1e-15 量级），说明浮点误差略超阈值，把断言阈值放宽到 `< 1e-10`；其余任何失败都是实现问题，不要放宽断言。

- [ ] **Step 6: .gitignore 追加 pytest 缓存**

在 `.gitignore` 的 `__pycache__/` 下一行插入：

```
.pytest_cache/
```

- [ ] **Step 7: 提交**

```bash
cd "e:/BM/迭代模型/损失" && git add backend/losses.py tests/ .gitignore && git commit -m "损失层改为硬轮廓：新增源图/输出两个轮廓函数，直方图收窄到轮廓内

轮廓由 k 锁死，两侧 mask 像素数严格相等；mask 是常量，梯度经 prediction 回流。
权重降为两项，删除 dynamic_contour、soft_histogram_cdf_loss、contour_area_ratio_loss。

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

### Task 2: 训练脚本接线、k=0 守卫与 CLI 新参数

**Files:**
- Modify: `backend/config.py:1`（`IMAGE_PATH`）与 `:25-27`（SHARE 常量段）
- Modify: `backend/train.py`（导入、`HISTORY_KEYS`、`parse_args`、`main` 主循环与离线指标）
- Modify: `backend/visualization.py:54-79`（`plot_support` 标题、`plot_source_contour_explanation` 改写）与 `:82-90`（收敛面板）
- Create: `images/`，移入 `123.png`、`567.png`
- Modify: `.gitignore:3-4`

**Interfaces:**
- Consumes: Task 1 的 `source_contour`、`topk_contour`、`soft_cdf`、`masked_histogram_cdf_loss`、`background_loss`、`CalibratedWeights(share_histogram, share_background)`
- Produces:
  - `train.py` CLI 新增 `--share-histogram`、`--share-background`
  - `HISTORY_KEYS` 去掉 `area_ratio`，长度 12 → 11
  - `visualization.plot_source_contour_explanation(source, contour, padding, ratio, save_path)` 签名不变，内部改画硬 mask
  - `config.SHARE_HISTOGRAM = 0.5`、`config.SHARE_BACKGROUND = 0.10`，`SHARE_AREA_RATIO` 已删除

- [ ] **Step 1: 建 images/ 并移入图片**

```bash
cd "e:/BM/迭代模型/损失" && mkdir -p images && mv 123.png 567.png images/ && ls images/
```

Expected: 输出 `123.png` 和 `567.png`。两者本来就在 `.gitignore` 里，移动不产生 git 变更。

- [ ] **Step 2: .gitignore 把两个文件名换成目录**

把这两行：

```
123.png
567.png
```

替换成：

```
images/
```

- [ ] **Step 3: 改 backend/config.py**

第 1 行改为：

```python
IMAGE_PATH = "images/567.png"
```

把第 15-27 行整段（`# 四项损失均为固定权重罚项` 开头到 `SHARE_AREA_RATIO = 0.5` 结尾）替换为：

```python
# 三项损失均为固定权重罚项：振幅是目标，权重恒 1；另两项的权重由首次前向的
# 实测损失自动标定，无需按图片重调：
#     β_i = SHARE_i × L_amp,0 / L_i,0
# 其中 SHARE_i 是该项初始贡献相对振幅项的目标占比。换图片时各项损失值都变，
# 算出的 β 跟着变，但贡献占比不变——所以下面两个常数是全局的，不是每图参数。
# SHARE 依据各约束的可信度（振幅最可信故权重最大）：
#   振幅         实测傅里叶振幅，唯一的硬数据                → 1（恒定）
#   轮廓内直方图  源图轮廓内的亮度分布，抗塌缩主力            → 大
#   背景         由网络自己输出的轮廓推出，自指、最不可信      → 最小
# 直方图接过了原轮廓占比项的主力位置：输出全黑时 mask 内是集中在 0 的分布，
# 而源图 mask 内全是中高亮度，CDF 差距被最大化，罚得比占比那个标量约束更狠。
SHARE_HISTOGRAM = 0.5       # 轮廓内直方图项初始贡献占振幅项的 50%
SHARE_BACKGROUND = 0.10     # 背景项占 10%：它最不可信，且过大会把整图压黑导致塌缩
```

- [ ] **Step 4: 改 backend/train.py 的导入与 HISTORY_KEYS**

第 15-17 行的导入替换为：

```python
from losses import (CalibratedWeights, amplitude_mask, background_loss,
                    masked_histogram_cdf_loss, normalized_log_amplitude_loss,
                    soft_cdf, source_contour, topk_contour)
```

第 27-28 行的 `HISTORY_KEYS` 替换为（去掉 `area_ratio`）：

```python
HISTORY_KEYS = ("iteration", "total", "amplitude", "histogram", "background",
                "psnr", "ssim", "pearson_cc", "amp_cc", "phase_error", "support_iou")
```

- [ ] **Step 5: 改 parse_args，新增两个 SHARE 参数并改阈值帮助文本**

把 `--contour-threshold` 那两行（`parse_args` 内第 151-152 行）替换为：

```python
    parser.add_argument("--contour-threshold", type=float, default=config.CONTOUR_THRESHOLD,
                        help="源图轮廓的相对峰值阈值（只用于标定源图 mask 与像素数 k）")
    parser.add_argument("--share-histogram", type=float, default=config.SHARE_HISTOGRAM,
                        help="轮廓内直方图项初始贡献占振幅项的比例")
    parser.add_argument("--share-background", type=float, default=config.SHARE_BACKGROUND,
                        help="背景项初始贡献占振幅项的比例")
```

- [ ] **Step 6: 改 main 的准备段：算 k、守卫 k=0、缓存目标 CDF**

把第 174-178 行（`source_contour = dynamic_contour(...)` 到 `config.SHARE_AREA_RATIO).to(device)`）替换为：

```python
    source_mask = source_contour(source, contour_sigma, contour_threshold)
    contour_pixels = int(source_mask.sum().item())
    if contour_pixels == 0:
        raise SystemExit("源图轮廓为空（阈值 {} 过高），无法标定像素数 k。".format(contour_threshold))
    source_area_ratio = contour_pixels / source_mask.numel()
    target_cdf = soft_cdf(source[source_mask > 0.5], config.HISTOGRAM_BINS,
                          config.HISTOGRAM_SOFTNESS).detach()
    model = UNet(config.BASE_CHANNELS).to(device)
    weights = CalibratedWeights(args.share_histogram, args.share_background).to(device)
```

注意 `source_area_ratio` 现在是 Python float，不再是 tensor。

- [ ] **Step 7: 改准备段的打印**

第 189 行替换为（多打一个 k，方便和预览接口对账）：

```python
    print("Source contour ratio: {:.2%} ({} px)".format(source_area_ratio, contour_pixels))
```

- [ ] **Step 8: 改主循环的三项损失**

把第 196-202 行（`histogram = soft_histogram_cdf_loss(...)` 到 `total = weights.total(...)`）替换为：

```python
        contour = topk_contour(prediction, contour_sigma, contour_pixels)
        histogram = masked_histogram_cdf_loss(prediction, contour, target_cdf,
                                              config.HISTOGRAM_BINS, config.HISTOGRAM_SOFTNESS)
        background = background_loss(prediction, contour)
        if iteration == 1:
            weights.calibrate(amplitude, histogram, background)
        total = weights.total(amplitude, histogram, background)
```

轮廓要在直方图之前算出来——两项都用它。

- [ ] **Step 9: 改离线指标段的下标**

`HISTORY_KEYS` 少了一项，`values` 元组随之从 12 元变 11 元，所有硬编码下标都要跟着挪。把第 210-227 行整段替换为：

```python
                evaluation_contour = topk_contour(evaluation_prediction, contour_sigma, contour_pixels)
                amp_cc, phase_error = amplitude_metrics(evaluation_prediction, source)
                values = (iteration, total.item(), amplitude.item(), histogram.item(), background.item(),
                          psnr(evaluation_prediction, source), ssim(evaluation_prediction, source),
                          pearson_cc(evaluation_prediction, source), amp_cc, phase_error,
                          support_iou(evaluation_contour, source_mask))
            for key, value in zip(history, values):
                history[key].append(value)
            elapsed = time.time() - start_time
            eta = elapsed / iteration * (args.iterations - iteration)
            ssim_value = values[6]
            iou = values[10]
            print("[{}/{}] total={:.3e} amp={:.3e} hist={:.3e} bg={:.3e} "
                  "ssim={:.3f} iou={:.3f} elapsed={} eta={}".format(
                      iteration, args.iterations, *values[1:5],
                      ssim_value, iou,
                      format_duration(elapsed), format_duration(eta)))
            emit_metric(args.stream_metrics, iteration=iteration, total=total.item(),
                        iou=iou, ssim=ssim_value)
```

- [ ] **Step 10: 改收尾段的最终轮廓与 plot_support 调用**

把第 232 行替换为：

```python
        final_contour = topk_contour(display_prediction, contour_sigma, contour_pixels)
```

第 235 行的 `plot_support(source_contour, final_contour, ...)` 改为传新变量名：

```python
    plot_support(source_mask, final_contour, padding, os.path.join(output_dir, "support.png"))
```

- [ ] **Step 11: 改 visualization.py 的收敛面板（11 → 10）**

把第 83-86 行的 `panels` 替换为：

```python
    panels = (("total", "总损失"), ("amplitude", "振幅损失"), ("histogram", "轮廓内直方图损失"),
              ("background", "背景损失"),
              ("psnr", "PSNR (dB)"), ("ssim", "SSIM"), ("pearson_cc", "Pearson CC"),
              ("amp_cc", "振幅域 CC"), ("phase_error", "平均相位误差 (rad)"), ("support_iou", "粗轮廓 IoU"))
```

第 90 行的 `log_keys` 去掉 `area_ratio`：

```python
    log_keys = ("amplitude", "histogram", "background")
```

- [ ] **Step 12: 改 plot_support 的标题措辞**

第 56-57 行替换为（"软"改"硬"，并去掉已不存在的"占比目标"）：

```python
    panels = ((source_contour, "原图硬轮廓（仅展示/像素数基准）"),
              (prediction_contour, "最终输出的 top-k 硬轮廓"))
```

- [ ] **Step 13: 改 plot_source_contour_explanation 的子图标题**

第 71-72 行替换为：

```python
    panels = ((source_image, "处理后的原图（灰度、反转、扩画布、pad）"),
              (contour_image, "二值化轮廓 mask（占比 {:.2%}）".format(ratio)))
```

- [ ] **Step 14: 端到端短跑验证**

```bash
cd "e:/BM/迭代模型/损失" && D:/anaconda3/envs/use/python.exe backend/train.py --image images/567.png --expand 1 --iterations 20 --log-every 5
```

Expected:
- 打印 `Source contour ratio: XX.XX% (NNNN px)`
- 每 5 轮一行 `[n/20] total=... amp=... hist=... bg=... ssim=... iou=...`（**没有** `area=` 字段）
- 最后打印 `Results saved to: results/run_567_x1_...`
- 不出现任何 traceback

- [ ] **Step 15: 检查产物列数与面板数**

```bash
cd "e:/BM/迭代模型/损失" && D:/anaconda3/envs/use/python.exe -c "
import csv, glob, os
run = max(glob.glob('results/run_567_x1_*'), key=os.path.getmtime)
with open(os.path.join(run, 'history.csv'), encoding='utf-8-sig') as f:
    header = next(csv.reader(f))
print('列数', len(header))
print('列名', header)
assert len(header) == 11, '应为 11 列'
assert 'area_ratio' not in header, 'area_ratio 未清除'
for name in ('real_space.png', 'spectra.png', 'support.png', 'convergence.png', 'metrics.csv', 'state.pt'):
    assert os.path.exists(os.path.join(run, name)), name
print('产物齐全:', run)"
```

Expected: `列数 11`，列名里没有 `area_ratio`，最后打印 `产物齐全: results/run_567_x1_...`。

- [ ] **Step 16: 目视确认 convergence.png 是 10 个面板**

打开上一步输出目录里的 `convergence.png`，确认：4×3 网格里有 10 个有内容的面板（右下 2 格空白），且没有"轮廓占比损失"面板，直方图面板标题为"轮廓内直方图损失"。

- [ ] **Step 17: 确认 losses 测试仍全绿**

```bash
cd "e:/BM/迭代模型/损失" && D:/anaconda3/envs/use/python.exe -m pytest tests/ -q
```

Expected: 9 passed。

- [ ] **Step 18: 提交**

```bash
cd "e:/BM/迭代模型/损失" && git add backend/train.py backend/visualization.py .gitignore && git commit -m "训练脚本接入硬轮廓，图片移入 images/

k 在准备段一次算定并守卫 k=0；目标 CDF 预先缓存；离线指标与收敛面板去掉占比项。
新增 --share-histogram / --share-background 两个 CLI 参数。

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

注意 `backend/config.py` 在 `.gitignore` 里，`git add` 不会带上它，这是预期的。

---

### Task 3: 服务端图片目录、预览拼图与 SHARE 透传

**Files:**
- Modify: `backend/visualization.py:1-11`（matplotlib 后端）
- Modify: `backend/server.py:29-30`（导入）、`:32-34`（路径常量）、`:80-97`（两个 handler）、`:153-161`（命令拼装）

**Interfaces:**
- Consumes: Task 1 的 `source_contour`；Task 2 的 `visualization.plot_source_contour_explanation`、`train.py` 的 `--share-*` 参数
- Produces:
  - `GET /api/images` → `{"images": ["123.png", "567.png"]}`（纯文件名，不带目录）
  - `POST /api/preview` 请求体新增无变化，响应体从 `{"image": ...}` 变成 `{"image": "data:image/png;base64,...", "ratio": 0.1234}`
  - 子进程命令新增 `--share-histogram`、`--share-background`

- [ ] **Step 1: 给 visualization.py 指定非交互后端**

当前后端是 `qtagg`（交互式）。`server.py` 在 tornado 的工作线程里调 matplotlib 出图，交互式后端在非主线程会报错或卡住。在 `import matplotlib.pyplot` 之前强制 Agg。

把 `backend/visualization.py` 第 1-6 行：

```python
import csv

import matplotlib.pyplot as plt
import numpy as np
import torch
from matplotlib.ticker import NullFormatter
```

替换为：

```python
import csv

import matplotlib

# 服务端在工作线程里出图，交互式后端（qtagg）在非主线程会失败，必须先切 Agg。
matplotlib.use("Agg")

import matplotlib.pyplot as plt  # noqa: E402
import numpy as np  # noqa: E402
import torch  # noqa: E402
from matplotlib.ticker import NullFormatter  # noqa: E402
```

- [ ] **Step 2: 改 server.py 的导入与路径常量**

第 29-30 行替换为：

```python
from losses import source_contour  # noqa: E402
from train import METRIC_PREFIX, RESULT_PREFIX, load_source  # noqa: E402
from visualization import plot_source_contour_explanation  # noqa: E402
```

第 32-34 行替换为（新增 `IMAGES` 常量）：

```python
ROOT = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
FRONTEND = os.path.join(ROOT, "frontend")
IMAGES = os.path.join(ROOT, "images")
PORT = 8770
```

- [ ] **Step 3: 改 ImagesHandler 扫 images/ 目录**

第 80-84 行替换为：

```python
class ImagesHandler(tornado.web.RequestHandler):
    def get(self):
        if not os.path.isdir(IMAGES):
            self.write({"images": []})
            return
        files = sorted(f for f in os.listdir(IMAGES) if IMAGE_RE.search(f))
        self.write({"images": files})
```

返回纯文件名；前端负责拼上 `images/` 前缀。

- [ ] **Step 4: 改 PreviewHandler 出拼图并返回 ratio**

第 86-97 行整个 `PreviewHandler` 替换为：

```python
class PreviewHandler(tornado.web.RequestHandler):
    def post(self):
        req = json.loads(self.request.body)
        source, padding = load_source(req["image"], "cpu", req["expand"])
        sigma = float(req.get("contour_sigma", config.CONTOUR_SIGMA))
        threshold = float(req.get("contour_threshold", config.CONTOUR_THRESHOLD))
        mask = source_contour(source, sigma, threshold)
        ratio = mask.sum().item() / mask.numel()
        buf = io.BytesIO()
        plot_source_contour_explanation(source, mask, padding, ratio, buf)
        self.write({"image": "data:image/png;base64," + base64.b64encode(buf.getvalue()).decode(),
                    "ratio": ratio})
```

`plot_source_contour_explanation` 的 `save_path` 传 `BytesIO` 而非路径——`fig.savefig` 支持 file-like 对象，但需要显式格式。下一步补这个。

- [ ] **Step 5: 让 plot_source_contour_explanation 支持写入 BytesIO**

`backend/visualization.py` 里该函数的 `fig.savefig(save_path, dpi=150, bbox_inches="tight")` 一行替换为：

```python
    fig.savefig(save_path, format="png", dpi=150, bbox_inches="tight")
```

显式给 `format="png"`：写文件路径时 matplotlib 从后缀推断格式，写 file-like 对象时推断不出来会报错。

- [ ] **Step 6: 改子进程命令，透传两个 SHARE**

第 158-160 行之间，在 `--contour-threshold` 之后、`--iterations` 之前插入两行：

```python
        cmd += ["--share-histogram", str(exp["share_histogram"])]
        cmd += ["--share-background", str(exp["share_background"])]
```

改完这段应是：

```python
        cmd += ["--image", exp["image"]]
        cmd += ["--expand", str(exp["expand"])]
        cmd += ["--contour-sigma", str(exp["contour_sigma"])]
        cmd += ["--contour-threshold", str(exp["contour_threshold"])]
        cmd += ["--share-histogram", str(exp["share_histogram"])]
        cmd += ["--share-background", str(exp["share_background"])]
        cmd += ["--iterations", str(exp["iterations"])]
        cmd += ["--stream-metrics"]
```

- [ ] **Step 7: 不启服务，直接验证两个接口的核心逻辑**

```bash
cd "e:/BM/迭代模型/损失" && D:/anaconda3/envs/use/python.exe -c "
import sys, os, io, json
sys.path.insert(0, 'backend')
from losses import source_contour
from train import load_source
from visualization import plot_source_contour_explanation
import config

# ImagesHandler 的扫描逻辑
import re
IMAGE_RE = re.compile(r'\.(png|jpg|jpeg|bmp|tif|tiff)\$', re.IGNORECASE)
files = sorted(f for f in os.listdir('images') if IMAGE_RE.search(f))
print('扫到图片:', files)
assert files == ['123.png', '567.png'], files

# PreviewHandler 的出图逻辑
source, padding = load_source('images/567.png', 'cpu', 1)
mask = source_contour(source, config.CONTOUR_SIGMA, config.CONTOUR_THRESHOLD)
ratio = mask.sum().item() / mask.numel()
buf = io.BytesIO()
plot_source_contour_explanation(source, mask, padding, ratio, buf)
print('占比 %.4f%%' % (ratio * 100), 'PNG 字节数', len(buf.getvalue()))
assert buf.getvalue()[:8] == b'\x89PNG\r\n\x1a\n', '不是合法 PNG'
print('预览出图 OK')"
```

Expected: 打印 `扫到图片: ['123.png', '567.png']`、一个占比百分数与 PNG 字节数（应是几十到几百 KB），最后 `预览出图 OK`。

- [ ] **Step 8: 核对预览占比与训练脚本一致**

```bash
cd "e:/BM/迭代模型/损失" && D:/anaconda3/envs/use/python.exe backend/train.py --image images/567.png --expand 1 --iterations 1 --log-every 1 2>&1 | grep "Source contour ratio"
```

Expected: 打印的百分数与上一步 `占比 XX.XXXX%` 前四位小数一致（同参数、同代码路径）。

- [ ] **Step 9: 提交**

```bash
cd "e:/BM/迭代模型/损失" && git add backend/server.py backend/visualization.py && git commit -m "服务端改扫 images/ 目录，预览接口改出对比拼图并返回占比

matplotlib 切 Agg 后端，避免工作线程里用交互式后端出图失败。
子进程命令透传两个 SHARE 参数。

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

### Task 4: 前端参数区四行与宽度交给布局

**Files:**
- Modify: `frontend/style.css:162-192`（`.field`、`.fields-row`、输入控件宽度）
- Modify: `frontend/app.js:164-231`（参数行构建）、`:280-288`（返回对象）、`:295-303`（`getConfig`）

**Interfaces:**
- Consumes: Task 3 的 `/api/images` 返回纯文件名、子进程需要 `share_histogram` / `share_background` 字段
- Produces:
  - `getConfig(exp)` 返回 `{image, expand, iterations, contour_sigma, contour_threshold, share_histogram, share_background}`，其中 `image` 是 `"images/567.png"` 形式的相对路径
  - `createRow` 返回对象新增 `shareHistInput`、`shareBgInput` 两个字段

- [ ] **Step 1: 改 style.css 三条规则**

第 162-166 行的 `.field` 替换为：

```css
.field {
  display: flex;
  flex-direction: column;
  gap: 4px;
  flex: 1;
  min-width: 0;
}
```

`min-width: 0` 必须有：否则 `<select>` 的内容宽度会顶破 flex 收缩，第 1 行撑不满、后面几行也分不均。

第 174-184 行的 `select, input[type="text"]` 规则里追加一行 `width: 100%;`：

```css
select, input[type="text"] {
  font-family: var(--font);
  font-size: 14px;
  padding: 6px 10px;
  border: 1px solid var(--hairline);
  border-radius: var(--radius-sm);
  background: var(--canvas);
  color: var(--ink);
  outline: none;
  transition: border-color 0.15s;
  width: 100%;
}
```

`box-sizing: border-box` 已在第 20 行全局生效，所以 `width: 100%` 含 padding 与边框，不会溢出。

第 188-192 行的 `.fields-row` 替换为（去掉 `flex-wrap`，每行只放一到两个，不该换行）：

```css
.fields-row {
  display: flex;
  gap: 14px;
}
```

- [ ] **Step 2: 重排 app.js 的参数行**

把 `createRow` 里第 164-231 行（从 `// Row 1: image + expand` 注释到 `params.append(top, fieldsRow1, fieldsRow2);`）整段替换为：

```javascript
  // 第 1 行：图片（独占整行）
  const fieldsRow1 = document.createElement("div");
  fieldsRow1.className = "fields-row";

  const imgWrap = document.createElement("div");
  imgWrap.className = "field";
  imgWrap.innerHTML = '<span class="field-label">图片</span>';
  const imgSel = document.createElement("select");
  imageList.forEach(f => {
    const opt = document.createElement("option");
    opt.value = f;
    opt.textContent = f;
    imgSel.appendChild(opt);
  });
  imgWrap.appendChild(imgSel);
  fieldsRow1.appendChild(imgWrap);

  // 第 2 行：画布扩大 + 训练轮数
  const fieldsRow2 = document.createElement("div");
  fieldsRow2.className = "fields-row";

  const expWrap = document.createElement("div");
  expWrap.className = "field";
  expWrap.innerHTML = '<span class="field-label">画布扩大</span>';
  const expInput = document.createElement("input");
  expInput.type = "text";
  expInput.value = "1";
  expWrap.appendChild(expInput);

  const iterWrap = document.createElement("div");
  iterWrap.className = "field";
  iterWrap.innerHTML = '<span class="field-label">训练轮数</span>';
  const iterInput = document.createElement("input");
  iterInput.type = "text";
  iterInput.value = "3000";
  iterWrap.appendChild(iterInput);

  fieldsRow2.append(expWrap, iterWrap);

  // 第 3 行：高斯半径 + 轮廓阈值
  const fieldsRow3 = document.createElement("div");
  fieldsRow3.className = "fields-row";

  const sigmaWrap = document.createElement("div");
  sigmaWrap.className = "field";
  sigmaWrap.innerHTML = '<span class="field-label">高斯半径</span>';
  const sigmaInput = document.createElement("input");
  sigmaInput.type = "text";
  sigmaInput.value = "16";
  sigmaWrap.appendChild(sigmaInput);

  const threshWrap = document.createElement("div");
  threshWrap.className = "field";
  threshWrap.innerHTML = '<span class="field-label">轮廓阈值</span>';
  const threshInput = document.createElement("input");
  threshInput.type = "text";
  threshInput.value = "0.20";
  threshWrap.appendChild(threshInput);

  fieldsRow3.append(sigmaWrap, threshWrap);

  // 第 4 行：直方图权重 + 背景权重（相对振幅项的初始贡献占比）
  const fieldsRow4 = document.createElement("div");
  fieldsRow4.className = "fields-row";

  const shareHistWrap = document.createElement("div");
  shareHistWrap.className = "field";
  shareHistWrap.innerHTML = '<span class="field-label">直方图权重</span>';
  const shareHistInput = document.createElement("input");
  shareHistInput.type = "text";
  shareHistInput.value = "0.5";
  shareHistWrap.appendChild(shareHistInput);

  const shareBgWrap = document.createElement("div");
  shareBgWrap.className = "field";
  shareBgWrap.innerHTML = '<span class="field-label">背景权重</span>';
  const shareBgInput = document.createElement("input");
  shareBgInput.type = "text";
  shareBgInput.value = "0.10";
  shareBgWrap.appendChild(shareBgInput);

  fieldsRow4.append(shareHistWrap, shareBgWrap);

  params.append(top, fieldsRow1, fieldsRow2, fieldsRow3, fieldsRow4);
```

原来那七处 `xxxInput.style.width = "..."` 以及 `min` / `max` / `step` 属性都不再需要——宽度由 CSS 均分，而这些控件是 `type="text"`，`min`/`max`/`step` 对文本框本来就无效（原代码里设了也没生效）。

- [ ] **Step 3: 改 createRow 的返回对象**

第 280-287 行的返回对象替换为（新增两个字段）：

```javascript
  return {
    rootEl: root,
    imgSel, expInput, iterInput, sigmaInput, threshInput,
    shareHistInput, shareBgInput,
    statusEl: status,
    previewBtn, realBtn,
    lossChart, iouChart,
    outputDir: null,
  };
```

- [ ] **Step 4: 改 getConfig**

第 295-303 行替换为：

```javascript
function getConfig(exp) {
  return {
    image: "images/" + exp.imgSel.value,
    expand: parseInt(exp.expInput.value, 10) || 1,
    iterations: parseInt(exp.iterInput.value, 10) || 1,
    contour_sigma: parseFloat(exp.sigmaInput.value) || 16,
    contour_threshold: parseFloat(exp.threshInput.value) || 0.2,
    share_histogram: parseFloat(exp.shareHistInput.value) || 0.5,
    share_background: parseFloat(exp.shareBgInput.value) || 0.1,
  };
}
```

下拉里存的是纯文件名，这里拼上 `images/` 前缀。子进程 `cwd=ROOT`，相对路径直接可用；预览接口的 `load_source` 也走同一个相对路径。

- [ ] **Step 5: 语法检查**

```bash
cd "e:/BM/迭代模型/损失" && D:/anaconda3/envs/use/python.exe -c "
import re, pathlib
src = pathlib.Path('frontend/app.js').read_text(encoding='utf-8')
# 七处写死宽度应已清空
leftover = re.findall(r'style\.width\s*=', src)
assert not leftover, '仍有写死宽度: %d 处' % len(leftover)
# 四行都在
for name in ('fieldsRow1', 'fieldsRow2', 'fieldsRow3', 'fieldsRow4'):
    assert src.count(name) >= 2, name
assert 'params.append(top, fieldsRow1, fieldsRow2, fieldsRow3, fieldsRow4)' in src
assert 'share_histogram' in src and 'share_background' in src
assert '\"images/\" + exp.imgSel.value' in src
print('app.js 结构检查通过')"
```

Expected: `app.js 结构检查通过`。

- [ ] **Step 6: 目视验证界面**

```bash
cd "e:/BM/迭代模型/损失" && cmd //c start start.vbs
```

浏览器自动打开 `http://localhost:8770`。确认：

- 参数区四行，第 1 行图片下拉撑满整列宽，下拉里是 `123.png` / `567.png`（纯文件名）。
- 第 2/3/4 行各两个输入框，各占一半宽，右边缘与第 1 行下拉的右边缘对齐。
- 第 4 行是「直方图权重 0.5」「背景权重 0.10」。
- 右侧两张图表仍在，没有被撑变形。

关掉浏览器标签，服务会自行退出（10 秒空闲后）。

- [ ] **Step 7: 提交**

```bash
cd "e:/BM/迭代模型/损失" && git add frontend/app.js frontend/style.css && git commit -m "前端参数区改四行，新增两个 SHARE 输入框

宽度交给 flex 均分，删掉七处写死的 style.width。图片下拉显示纯文件名，
getConfig 拼上 images/ 前缀。

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

### Task 5: 预览按钮改名与显示占比

**Files:**
- Modify: `frontend/app.js:122-130`（`fetchPreview`）、`:254-264`（按钮）、`:320-332`（`doPreview`）、`:445`、`:481`（两处重置）

**Interfaces:**
- Consumes: Task 3 的 `/api/preview` 返回 `{image, ratio}`；Task 4 的 `getConfig` 新字段
- Produces: 无（前端末端改动）

- [ ] **Step 1: 改 fetchPreview 返回整个响应体**

第 122-130 行替换为（原来只返回 `data.image`，现在还要 `ratio`）：

```javascript
async function fetchPreview(image, expand, contourSigma, contourThreshold) {
  const res = await fetch("/api/preview", {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify({ image, expand, contour_sigma: contourSigma, contour_threshold: contourThreshold }),
  });
  return await res.json();
}
```

- [ ] **Step 2: 按钮初始就叫「轮廓对比图」**

第 254-256 行替换为：

```javascript
  const previewBtn = document.createElement("button");
  previewBtn.className = "preview-btn";
  previewBtn.textContent = "轮廓对比图";
```

第 257-264 行的点击回调保持原逻辑不动（`outputDir` 有值走结果图、没值走预览），此步只改初始文案。

- [ ] **Step 3: 改 doPreview，标题带占比**

第 320-332 行替换为：

```javascript
async function doPreview(index) {
  const exp = experiments[index];
  const cfg = getConfig(exp);
  openModalLoading("轮廓对比图");
  try {
    const data = await fetchPreview(cfg.image, cfg.expand, cfg.contour_sigma, cfg.contour_threshold);
    document.getElementById("modal-title").textContent =
      "轮廓对比图 · 轮廓占比 " + (data.ratio * 100).toFixed(2) + "%";
    document.getElementById("preview-img").src = data.image;
    document.getElementById("modal-loading").classList.add("hidden");
  } catch (e) {
    appendTerminal("预览出错：" + e.message, true);
    document.getElementById("preview-modal").classList.add("hidden");
  }
}
```

标题先设成不带占比的版本（加载中还不知道占比），拿到响应后再补上数值。

- [ ] **Step 4: 清掉三处按钮改名代码**

按钮名不再随状态变化，这三行全是残留。

第 405 行（`handleMsg` 的 `exp_done` 分支内）删除这一行：

```javascript
          exp.previewBtn.textContent = "轮廓对比图";
```

删完该分支应是：

```javascript
    case "exp_done":
      if (exp) {
        setStatus(exp, "已完成", "status-done");
        if (msg.output_dir) {
          exp.outputDir = msg.output_dir;
          exp.realBtn.disabled = false;
        }
      }
      break;
```

第 445 行（`startExperiments` 内）删除这一行：

```javascript
    exp.previewBtn.textContent = "预览轮廓图";
```

第 481 行（`resetAll` 内）删除同样的一行。

删完 `startExperiments` 的 forEach 体应是：

```javascript
  experiments.forEach(exp => {
    setStatus(exp, "排队中", "status-idle");
    exp.lossChart.clear();
    exp.iouChart.clear();
    exp.outputDir = null;
    exp.realBtn.disabled = true;
  });
```

`resetAll` 的 forEach 体应是：

```javascript
  experiments.forEach(exp => {
    setStatus(exp, "待机", "status-idle");
    exp.lossChart.clear();
    exp.iouChart.clear();
    exp.outputDir = null;
    exp.realBtn.disabled = true;
  });
```

- [ ] **Step 5: 结构检查**

```bash
cd "e:/BM/迭代模型/损失" && D:/anaconda3/envs/use/python.exe -c "
import pathlib
src = pathlib.Path('frontend/app.js').read_text(encoding='utf-8')
assert '预览轮廓图' not in src, '仍有旧按钮文案'
assert src.count('轮廓对比图') == 4, '应恰好 4 处：按钮初值、结果图标题、doPreview 加载中标题、doPreview 带占比标题；实测 %d' % src.count('轮廓对比图')
assert src.count('previewBtn.textContent') == 1, '按钮文案只应在 createRow 里设一次初值；实测 %d 处' % src.count('previewBtn.textContent')
assert 'data.ratio' in src and 'toFixed(2)' in src
assert 'return await res.json();' in src
print('app.js 预览改动检查通过')"
```

Expected: `app.js 预览改动检查通过`。

- [ ] **Step 6: 目视验证两条分支**

```bash
cd "e:/BM/迭代模型/损失" && cmd //c start start.vbs
```

**训练前分支**：直接点「轮廓对比图」。确认弹窗标题为「轮廓对比图 · 轮廓占比 XX.XX%」，图内左边是处理后的原图（黑底、物体亮）、右边是纯黑白的二值 mask，子图标题里的占比与标题栏一致。

**训练后分支**：把训练轮数改成 `30`，点「开始运行」等它跑完，再点「轮廓对比图」。确认弹窗标题为「轮廓对比图」（不带占比），显示的是 `support.png`——左右两张 mask 对比图。这条分支的行为与改动前一致。

同时确认「效果对比图」按钮在跑完后变为可用。

关掉浏览器标签退出服务。

- [ ] **Step 7: 提交**

```bash
cd "e:/BM/迭代模型/损失" && git add frontend/app.js && git commit -m "预览按钮统一叫轮廓对比图，训练前显示原图与二值 mask 对比及占比

训练后仍显示 support.png，行为不变。删掉两处把按钮名改回旧文案的残留代码。

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

### Task 6: 同步 README 与端到端验收

**Files:**
- Modify: `README.md`（损失函数、目录结构、命令行、参数表、前端说明、主要参数六处）

**Interfaces:**
- Consumes: 前五个任务的全部改动
- Produces: 无

- [ ] **Step 1: 改 README 开头的损失函数章节**

把「## 损失函数」下面的四条编号列表（`1. 振幅损失` 到 `4. 轮廓占比损失`）整段替换为：

```markdown
1. 振幅损失：排除直流点和近零振幅点，在归一化对数振幅域计算 MSE，防止低频大数值主导。
2. 轮廓内直方图损失：只在轮廓内比较原图与输出的可微软 CDF 分布。目标分布取自源图轮廓内的像素值（训练前算一次并缓存），预测分布取自输出**自己的**轮廓内像素值——不用源图轮廓的位置，否则等于把轮廓形状当成监督。两侧像素数严格相等，无需长度补偿。
3. 背景损失：轮廓外的输出被约束为 0。轮廓是常量，网络不能通过扩大轮廓规避背景惩罚。

轮廓的算法分两侧：

- **源图侧**（训练前算一次）：高斯模糊 → 减去自身最低基线 → 峰值归一化 → 硬阈值二值化，得到 0/1 的 mask。数出其中 1 的个数记为 `k`，占比 `ρ = k / N` 只用于显示。减基线是为了避免整体亮度把轮廓撑到全图；阈值是相对峰值的比例，不随图片整体亮度漂移。
- **输出侧**（每轮）：高斯模糊 → 取前 `k` 个最亮像素置 1。像素数恒为 `k`，与源图轮廓严格相等。

锁 `k` 而不是锁 `ρ`：后者要经两次浮点取整，两边像素数会差几个。用 topk 的下标散射而不是阈值比较，也是为了这个严格性——并列值在阈值下会多圈几个像素。

mask 的 `requires_grad` 为 `False`，是纯常量，只当选择器用。它本身几乎处处梯度为 0、不可导，但两个消费者都只需要"选中哪些像素"这个信息，梯度经 `prediction` 本身回流到网络。原先的轮廓占比损失需要可导轮廓（它算 `d(contour.mean())/d(pred)`），而在 top-k 构造下占比恒等相等、该损失恒为 0，因此已删除。

图片统一采用反转后的工作表示：黑色背景为 0，物体为亮色。
```

原来的「图片统一采用反转后的工作表示」那一行已并入上面，不要重复保留。

- [ ] **Step 2: 改目录结构**

把「## 目录结构」代码块里的这几行：

```
    losses.py          四项损失函数 + CalibratedWeights
```

改为：

```
    losses.py          三项损失函数 + 两侧轮廓 + CalibratedWeights
```

把这两行：

```
  123.png / 567.png    数据图片（放根目录，不入库）
  results/             训练产物（不入库）
  start.bat            双击启动 Web 服务
```

改为：

```
  images/              数据图片（123.png / 567.png，不入库）
  tests/
    conftest.py        让测试能 import backend 模块
    test_losses.py     轮廓与损失的不变式断言
  results/             训练产物（不入库）
  start.vbs            双击启动 Web 服务
```

`start.bat` 改成 `start.vbs` 是修一处既有的过时描述——实际文件是 `start.vbs`。

- [ ] **Step 3: 改命令行示例**

把「## 命令行运行」下的三段命令替换为：

````markdown
```powershell
# 567：不扩大画布
D:\anaconda3\envs\use\python.exe backend\train.py --image images/567.png --expand 1 --iterations 3000

# 123：画布扩大 2 倍（物体贴边，需过采样裕量避免 FFT wraparound）
D:\anaconda3\envs\use\python.exe backend\train.py --image images/123.png --expand 2 --iterations 3000

# 自定义高斯半径（控制轮廓提取的模糊尺度）
D:\anaconda3\envs\use\python.exe backend\train.py --image images/567.png --contour-sigma 8 --iterations 3000

# 自定义两项损失的初始贡献占比
D:\anaconda3\envs\use\python.exe backend\train.py --image images/567.png --share-histogram 0.5 --share-background 0.10
```
````

- [ ] **Step 4: 改参数表**

在参数表里，把 `--contour-threshold` 那一行的说明改掉，并在它后面插入两行新参数：

```markdown
| `--contour-threshold` | `config.CONTOUR_THRESHOLD` (0.20) | 源图轮廓的相对峰值阈值。**只在训练前用一次**，用来二值化源图 mask 并数出像素数 `k`；输出侧不用阈值，改按 `k` 取 top-k |
| `--share-histogram` | `config.SHARE_HISTOGRAM` (0.5) | 轮廓内直方图项初始贡献占振幅项的比例 |
| `--share-background` | `config.SHARE_BACKGROUND` (0.10) | 背景项初始贡献占振幅项的比例 |
```

同时把 `--contour-sigma` 那行的说明里 `dynamic_contour` 改成"两侧轮廓"：

```markdown
| `--contour-sigma` | `config.CONTOUR_SIGMA` (16) | 高斯模糊半径，控制两侧轮廓提取的尺度（源图与每轮输出都用同一个值） |
```

- [ ] **Step 5: 改结果目录说明**

把这一行：

```
结果目录包含：`real_space.png`（原图 / 输出 / 叠加 / 误差）、`spectra.png`（振幅谱）、`support.png`（粗轮廓对比）、`convergence.png`（11 个指标面板）、`history.csv`、`metrics.csv`、`state.pt`。
```

改为：

```
结果目录包含：`real_space.png`（原图 / 输出 / 叠加 / 误差）、`spectra.png`（振幅谱）、`support.png`（硬轮廓对比）、`convergence.png`（10 个指标面板）、`history.csv`、`metrics.csv`、`state.pt`。
```

- [ ] **Step 6: 改 Web 前端章节**

把「浏览器打开」下面的第一条和第三条替换为：

```markdown
- 顶部选择实验组数（1~6），每组一行参数设定：第一行图片（下拉列出 `images/` 里的文件），第二行画布扩大 + 训练轮数，第三行高斯半径 + 轮廓阈值，第四行直方图权重 + 背景权重。右上角「重置」按钮清除全部状态，方便重新配置。
```

```markdown
- 卡片底部为结果按钮区：「轮廓对比图」在训练前显示处理后的原图与二值化 mask 的对比（标题栏标注轮廓占比），训练完成后改为显示 `support.png`（源图轮廓 vs 输出轮廓）；「效果对比图」在训练完成后激活。
```

第二条（曲线）和第四、五条（终端、队列）不改。

- [ ] **Step 7: 改主要参数章节**

把「## 主要参数」整节替换为：

````markdown
## 主要参数

集中在 `backend/config.py`：默认训练 3000 轮；轮廓参数为 `sigma=16.0`、阈值为基线校正后峰值的 `0.20`，以形成整个分子的连续概括外包络。离线指标及最终对比图会先消除平移和镜像歧义，再与原图比较。

三项损失用固定权重相加。振幅是唯一的硬数据，权重恒为 1；其余两项的权重由第一次前向按目标贡献比自动标定，训练中保持不变，**不需要按图片重调**：

```
β_i = SHARE_i × 首步振幅损失 / 首步第 i 项损失
```

`SHARE_i` 是该项初始贡献相对振幅项的目标占比，按各约束的可信度排定：轮廓内直方图 `0.5`、背景 `0.10`，即振幅 > 直方图 > 背景。背景取值最小，因为它是由网络自己输出的轮廓推出来的、最不可信，且权重过大会把整幅图压黑而塌缩。换图片时各项损失值都变、算出的 β 跟着变，但贡献占比不变，所以这两个常数是全局的；前端第四行也可以逐组覆盖。

直方图接过了原轮廓占比项的抗塌缩主力位置（原为 `0.5`）：输出全黑时轮廓内是集中在 0 的分布，而源图轮廓内全是中高亮度，CDF 差距被最大化，罚得比占比那个标量约束更狠。施压分工也因此变干净——轮廓内由直方图 + 振幅负责，轮廓外由背景 + 振幅负责，不再有两项在全画布重叠拉扯。

先前曾给占比项配 AL-PINNs 增广拉格朗日乘子 λ（[arXiv:2205.01059](https://arxiv.org/abs/2205.01059)）做梯度上升自适应，但收敛末期 θ 与 λ 的原始-对偶追逐导致占比损失上下振荡，已改回固定权重；该项现已随硬轮廓改动一并删除。方法选型的完整论证、以及先前多次失败（Kendall 不确定性加权、背景配乘子、占比配乘子）的实测记录，见 `adaptive_weighting_research.md`（记录的是历史实验，其中的四损失结构已不是当前实现）。

## 测试

```powershell
D:\anaconda3\envs\use\python.exe -m pytest tests\ -v
```

覆盖轮廓的不变式（两侧 mask 像素数严格相等、top-k 选中的确是最亮像素、mask 是常量）、梯度能经 `prediction` 回流、以及权重标定后各项贡献等于 `SHARE × 振幅损失`。
````

- [ ] **Step 8: 全量测试**

```bash
cd "e:/BM/迭代模型/损失" && D:/anaconda3/envs/use/python.exe -m pytest tests/ -v
```

Expected: 9 passed。

- [ ] **Step 9: 两张图各跑一次短程，确认两种画布路径都通**

```bash
cd "e:/BM/迭代模型/损失" && D:/anaconda3/envs/use/python.exe backend/train.py --image images/567.png --expand 1 --iterations 30 --log-every 10 && D:/anaconda3/envs/use/python.exe backend/train.py --image images/123.png --expand 2 --iterations 30 --log-every 10
```

Expected: 两次都跑完并打印 `Results saved to: ...`，无 traceback。`123.png` 是 610×589 贴边图，`expand 2` 后画布约 1232×1184，这条路径的 `padding` 走 `(0,0,0,0)` 分支，要确认出图不报错。

- [ ] **Step 10: 端到端跑一组前端实验**

```bash
cd "e:/BM/迭代模型/损失" && cmd //c start start.vbs
```

在界面上：训练轮数改 `30`，直方图权重改 `0.4`（改一个非默认值，验证透传生效），点「开始运行」。确认：

- 终端面板能看到 `Source contour ratio: XX.XX% (NNNN px)` 和逐轮日志。
- 两条曲线（总损失、IoU）在动。
- 跑完后状态变「已完成」，「效果对比图」可点，「轮廓对比图」显示 `support.png`。
- 终端里**没有** `area=` 字段、没有任何 traceback。

关掉浏览器标签退出服务。

- [ ] **Step 11: 确认 git 状态干净、无遗漏**

```bash
cd "e:/BM/迭代模型/损失" && git status --short && echo "--- 残留引用检查 ---" && grep -rn "dynamic_contour\|contour_area_ratio_loss\|soft_histogram_cdf_loss\|area_ratio\|SHARE_AREA_RATIO" backend/ frontend/ README.md tests/ || echo "无残留引用"
```

Expected: `git status` 只列出 `README.md`（待提交）；残留引用检查输出 `无残留引用`。

若 `grep` 命中 `backend/config.py`，说明第 3 步的常量段没改干净，回去修（该文件已 gitignore，不会出现在 `git status` 里，容易漏）。

- [ ] **Step 12: 提交**

```bash
cd "e:/BM/迭代模型/损失" && git add README.md && git commit -m "同步 README：三项损失、两侧轮廓算法、images/ 目录与四行参数区

顺带修正两处过时描述：start.bat 实际是 start.vbs，收敛面板 11 个改 10 个。

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

## 验收清单

全部任务完成后，逐条核对：

- [ ] `python -m pytest tests/ -v` → 9 passed
- [ ] 两张图各跑 30 轮无报错（`567.png --expand 1`、`123.png --expand 2`）
- [ ] `history.csv` 11 列，无 `area_ratio`
- [ ] `convergence.png` 10 个有内容的面板，直方图面板标题为「轮廓内直方图损失」
- [ ] 前端参数区四行，第 1 行下拉撑满、后三行各两个框均分
- [ ] 训练前点「轮廓对比图」→ 标题带占比，显示原图 + 二值 mask
- [ ] 训练后点「轮廓对比图」→ 显示 `support.png`，行为与改动前一致
- [ ] 前端改 SHARE 值能透传到子进程（终端日志里损失量级随之变化）
- [ ] `grep` 全仓无 `dynamic_contour` / `contour_area_ratio_loss` / `soft_histogram_cdf_loss` / `area_ratio` / `SHARE_AREA_RATIO` 残留（`adaptive_weighting_research.md` 除外，那是历史记录）
