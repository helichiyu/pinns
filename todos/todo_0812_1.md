# TODO: Add input-output consistency loss - 2026-08-12

## 需求描述

为迭代模型新增输入与输出之间的 MSE 一致性损失：模型收敛时，相邻迭代结果应接近不变。
前端新增该损失的启用开关与权重输入；启用后其归一化、Kendall 自适应加权、训练记录和
实时曲线与直方图、背景损失一致。

## 实施方案

将当前轮模型输入作为 MSE 目标、当前输出作为预测值。损失始终记录以便观察，只有开关启用
时才以配置权重纳入总损失。扩展四项自适应权重、命令行、WebSocket 子进程参数、前端控件和
离线收敛图，并以单元测试验证损失及权重行为。

## 任务清单

### 阶段1: 训练损失

- [x] 任务1: 添加输入输出 MSE 损失及四项自适应加权支持
- [x] 任务2: 添加命令行配置、历史记录和离线收敛图

### 阶段2: 前端控制

- [x] 任务3: 添加启用开关、权重输入、服务端参数转发和实时曲线

### 阶段3: 验证

- [x] 任务4: 更新单元测试并运行测试套件

## 验收标准

- 开关启用时，输入输出 MSE 以指定权重参与四项归一化 Kendall 总损失。
- 开关关闭时，该项不影响总损失。
- 前端可配置开关和权重，实时曲线及收敛图展示该项。
- 所有测试通过。

## 执行记录

### 2026-08-12

- 创建 TODO；约定输入为当前前向的迭代输入，输出为同次前向结果。
- 完成训练、命令行、历史记录、离线图、WebSocket 和前端控件集成；开关默认关闭，默认权重为 0.10。
- 验证完成：`D:\anaconda3\envs\use\python.exe -m pytest tests -v` 通过（27 项）；Python 编译检查和前端 JavaScript 语法检查通过。
- 前端复选框文案调整为中文“启用输入输出 MSE”。
- 按用户最新要求更新 AGENTS.md：前端所有用户可见内容必须使用中文；新增损失的权重标签和曲线图例已同步中文化。

### 2026-08-12（PSNR 清理）

- 用户要求从 convergence 中移除 PSNR，并进一步删除其计算与记录；此变更记录在 `todo_0812_2.md`。
