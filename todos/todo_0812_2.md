# TODO: 移除 PSNR 指标 - 2026-08-12

## 需求描述

绘制 convergence 时不再展示 PSNR，图片恢复为 3×3 布局；删除 PSNR 的计算及其所有训练结果记录。

## 实施方案

从训练评估、历史字段、最终指标导出和离线收敛图面板中移除 PSNR，保留 SSIM、Pearson CC、振幅域 CC 和粗轮廓 IoU。

## 任务清单

### 阶段1: 移除 PSNR

- [x] 任务1: 删除 PSNR 计算、历史记录、最终指标与 convergence 面板

### 阶段2: 验证

- [进行中] 任务2: 运行测试并确认记录字段与面板数量

## 验收标准

- 训练中不再计算 PSNR。
- `history.csv` 和 `metrics.csv` 不包含 PSNR。
- `convergence.png` 为不含 PSNR 的 3×3 共 9 个面板。
- 测试套件通过。

## 执行记录

### 2026-08-12

- 已开始：确认前端无 PSNR 依赖，计划仅修改训练与离线可视化。
- 已完成任务1：从训练、`history.csv`、`metrics.csv` 与 convergence 面板移除 PSNR；保留的 9 个面板恢复为 3×3。
- 按用户要求略增每个 convergence 面板尺寸至 4.0×4.3 英寸。
