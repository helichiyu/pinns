# TODO: Ignore config.py in Git - 2026-08-05

## 需求描述

将仓库根目录下的 `config.py` 添加到 `.gitignore`，避免每次修改本地参数时产生 Git 变更。

## 实施方案

在 `.gitignore` 中添加 `/config.py`，仅忽略根目录的配置文件，不影响其他目录中的同名文件。

## 任务清单

### 阶段1: 配置更新

- [x] 任务1: 在 `.gitignore` 中添加 `/config.py`
- [x] 任务2: 验证 Git 忽略状态

## 验收标准

`git check-ignore config.py` 能匹配 `/config.py`，且 `.gitignore` 仅包含本次必要变更。

## 执行记录

### 2026-08-05

- 完成任务1: 添加根目录 `config.py` 忽略规则。
- 完成任务2: `git check-ignore -v config.py` 命中 `.gitignore` 中的 `/config.py`。
- 备注: `config.py` 原本已被 Git 跟踪，已从索引移除；本地文件保持不变。
