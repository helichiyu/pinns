# TODO: 123.png 相位恢复（蓝色主体 + 2 倍扩画布）- 2026-08-04

## 需求描述

将输入图从 567.png（黑色主体、自带黑边距）切换到 123.png（蓝色主体、四边贴边）。
两个差异需要处理：
1. 贴边 → FFT 卷绕风险（对比项目 C14）；需在输入网络前 2 倍扩画布满足过采样。
2. 蓝色主体 → `.convert("L")` 把蓝色压暗（反转后仅 0.573，对比度弱）；改用红通道反转 ρ = bg − R（蓝色吸红，反转后 ≈0.71，接近 567 的 0.752）。

约束：不修改 567 的现有代码（main.py / config.py / model.py / losses.py / visualization.py）；
为 123 新写入口，可调用 567 的辅助函数。

## 实施方案

新增 `main_123.py`：
- `load_source_123(path, device)`：
  1. 读 RGB，取红通道 R；
  2. background = 原始四边 R 的中位数；
  3. source = clip(bg − R, 0, 1)（明暗反转，蓝色主体变亮）；
  4. 2 倍扩画布（每维 ×2，居中零填充，过采样裕量）；
  5. pad 到 16 的倍数（UNet 4 级下采样）；
  6. 返回 padding = 扩画布边 + pad16（累计），便于可视化裁回原始物体区域。
- 训练循环与 567 `main()` 完全一致，仅替换 `load_source`；
  psnr/ssim/pearson_cc/amplitude_metrics/support_iou/register_to_source/random_phase_initialization
  全部从 `main` 导入复用；model / losses / visualization / config 直接复用。

## 任务清单

### 阶段1: 代码

- [x] 任务1: 新建 `main_123.py`（load_source_123 + 复用主循环）
- [x] 任务2: 烟雾测试（少量迭代验证可跑通）

### 阶段2: 验证

- [x] 任务3: 完整运行，产出 real_space / spectra / support / convergence
- [ ] 任务4: 用户确认效果图

## 验收标准

- 123.png 经红通道反转后主体亮、背景 ≈0；
- 2 倍扩画布后物体居中、四周有零裕量；
- 程序跑通不报错，产出四张图 + history.csv + metrics.csv + state.pt；
- 不修改 567 任何文件。

## 执行记录

### 2026-08-04

- 实测两图：567 主体黑灰（反转 0.752，不贴边）；123 主体蓝（L 反转仅 0.573，四边贴边）。
- 确认方案：红通道反转 + 2 倍扩画布 + pad16。
- 写 main_123.py，烟雾测试通过后完整运行。
